[build-system]
requires = ["setuptools>=61", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "godmax"
version = "0.1.0"
description = "Gas thermODynamics and Matter distribution using jAX (GODMAX)"
readme = "README.md"
authors = [
    {name = "Shivam Pandey", email = "shivampcosmo@gmail.com"}
]
license = {text = "MIT"}
classifiers = [
    "Development Status :: 3 - Alpha",
    "Intended Audience :: Science/Research",
    "Topic :: Scientific/Engineering :: Physics",
    "Topic :: Scientific/Engineering :: Astronomy",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.9",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "License :: OSI Approved :: MIT License",
    "Operating System :: OS Independent",
]
requires-python = ">=3.9"
dependencies = [
    "numpy>=2.0",
    "scipy>=1.11",
    "numpyro>=0.15",
    "jax>=0.4.25",
    "jaxlib>=0.4.25",
    "interpax>=0.3",
    "colossus",
    "configobj",
    "astropy>=5.0",
    "jax_cosmo",
    "deepmerge",
    "healpy",
]

[project.urls]
Homepage = "https://github.com/shivampcosmo/GODMAX"
Repository = "https://github.com/shivampcosmo/GODMAX"

[tool.setuptools.package-dir]
"" = "src"

[tool.setuptools.packages.find]
where = ["src"]

[project.optional-dependencies]
dev = ["pytest", "pytest-cov", "black", "flake8"]
